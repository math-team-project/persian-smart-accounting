"""لایه‌ی دسترسی به داده (repository) برای هر موجودیت داشبورد.

روترها و سرویس‌ها هرگز مستقیماً با ``Session``/مدل‌های ORM کار نمی‌کنند؛ همه‌ی
کوئری‌های داشبورد این‌جا متمرکز شده‌اند. مزیت عملی: تغییر پایگاه‌داده (مثلاً به
Postgres) یا تغییر شکل کوئری‌ها فقط همین پکیج را تحت تأثیر قرار می‌دهد.
"""
