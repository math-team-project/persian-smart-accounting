# Standalone dependencies for running audit_summarizer's CLI (main.py) on its
# own, outside of the main dashboard (which already installs everything it
# needs via the project's root-level requirements.txt).
python-docx>=1.1.0
pdfplumber>=0.11.0
pypdf>=4.0.0
pymupdf>=1.24.0            # primary PDF extractor (correct RTL reading order)
pytesseract>=0.3.10        # OCR fallback for scanned PDFs (requires Tesseract binary)
pdf2image>=1.17.0          # renders PDF pages for OCR (requires Poppler binary)
requests>=2.31.0
python-dotenv>=1.0.0
pytest>=8.0.0
