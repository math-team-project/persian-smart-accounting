"""کارگاه «تحلیل بودجه» -- موتور پایش، تحلیل و ارزیابی مدیریتی بودجه.

این پکیج مستقل از لایه‌ی وب است (هیچ ایمپورتی از ``api/`` ندارد) تا هم داخل
job پس‌زمینه‌ی داشبورد و هم به‌صورت مستقل/CLI قابل اجرا باشد. خط پردازش سه‌مرحله‌ای
است و هر مرحله مسئولیت جداگانه دارد:

    مرحله ۱ -- ``extraction``: خواندن اسناد (اکسل یا PDF چاپ‌شده از اکسل) و ساخت
        یک نمایش میانی ساختارمند (JSON) از فرم/بخش/ردیف/ستون/مقدار/واحد/سال
        همراه با محل سلول. هیچ مقداری حدس زده نمی‌شود؛ داده‌ی شناسایی‌نشده
        «موجود نیست» علامت می‌خورد.
    مرحله ۲ -- ``analysis``: تنها جایی که مدل زبانی صدا زده می‌شود. خروجی مدل
        اجباراً JSON دقیقاً منطبق بر ``schemas.BudgetAnalysisReport`` است (نه متن
        آزاد) و در صورت نامعتبر بودن، یک‌بار با متن خطای اعتبارسنجی تکرار می‌شود.
    مرحله ۳ -- ``report``: رندر کامل و قطعی (deterministic) همان JSON به یک فایل
        Word فارسی راست‌به‌چپ. هیچ قالب‌بندی‌ای به مدل زبانی سپرده نمی‌شود.

نکته‌ی معماری: کمک‌تابع‌های سطح-پایین RTL/font در ``docx_rtl`` از همان
ابزار موجود پروژه (``audit_report_generator.docx_builder``) بازاستفاده می‌شوند؛
منطق ساخت سند راست‌به‌چپ دوباره نوشته نشده است.

ماژول‌های این پکیج عمداً سبک نگه داشته شده‌اند: ``__init__`` هیچ ماژول سنگینی را
ایمپورت نمی‌کند تا import این پکیج (مثلاً برای دسترسی به ``MISSING_TEXT``) هزینه‌ی
بارگذاری pandas/docx را تحمیل نکند.
"""
from __future__ import annotations

__all__ = ["MISSING_TEXT", "NOT_COMPUTABLE_TEXT", "SLUG"]

# واژگان «داده‌ی موجود نیست» -- طبق اصول ۲ و ۳ پرامپت مرجع، این دو عبارت تنها
# شکل مجاز اعلام فقدان داده هستند و در همه‌ی لایه‌ها (استخراج، پرامپت، رندر)
# از همین یک منبع استفاده می‌شود.
MISSING_TEXT = "در اسناد موجود نیست"
NOT_COMPUTABLE_TEXT = "قابل محاسبه نیست ـ داده موردنیاز در اسناد موجود نیست"

# اسلاگ این کارگاه در رجیستری کارگاه‌ها (منبع واحد نام آن).
SLUG = "budget-analysis"
